/**
 * Token de inyección para el servicio SAP activo.
 *
 * Separado en su propio archivo para evitar referencias circulares
 * entre sap.module.ts y sap.controller.ts.
 */
export const SAP_SERVICE = 'SAP_SERVICE';